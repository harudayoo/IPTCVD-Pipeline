# Project profile

Every hook glob and doc mapping derives from this table.
A field is not CONFIRMED until its command has been executed successfully.

| Field | Value | Status |
|---|---|---|
| Project name | {{PROJECT_NAME}} | |
| Stack | {{STACK_LINE}} | |
| Package manager | {{PACKAGE_MANAGER}} | |
| Dev command | `{{DEV_COMMAND}}` | |
| Test command (non-watching) | `{{TEST_COMMAND}}` | |
| Single-test command | `{{SINGLE_TEST_COMMAND}}` | |
| Format command (fixes) | `{{FORMAT_COMMAND}}` | |
| Type-check command | `{{TYPECHECK_COMMAND}}` | |
| Build command | `{{BUILD_COMMAND}}` | |
| Dependency audit command | `{{DEPENDENCY_AUDIT_COMMAND}}` | |
| Source roots | {{SOURCE_ROOTS}} | |
| Front-end root | {{FRONTEND_ROOT}} | |
| Test root | {{TEST_ROOT}} | |
| Shared surfaces | {{SHARED_SURFACES}} | |
| Design token file | {{TOKEN_FILE}} | |
| Has UI | {{HAS_UI}} | |

## Shared surfaces

Comma-separated directories, relative to the repo root, where a NEW file has to
state what it reuses before it is created -- components, pages, services: the
places a fifth near-duplicate of an existing thing tends to appear. Editing an
existing file in them is never gated.

Leave blank to switch the reuse gate off. That is a decision, not a default:
"reuse what is already here" stays advisory until something asks.

## Verification log
Run every command above and record its exit code here.

| Command | Exit code | Notes |
|---|---|---|
